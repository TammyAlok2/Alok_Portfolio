/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["img.icons8.com", "cdn.jsdelivr.net", "cdn-icons-png.flaticon.com", "raw.githubusercontent.com"]
    }
};

export default nextConfig;
